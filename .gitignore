# dependencies
node_modules

# Next.js build output
.next
out

# TypeScript incremental build cache
*.tsbuildinfo

# environment files (the example is committed, real values never are)
.env*
!.env.example

# OS / editor
.DS_Store
Thumbs.db

# local Claude Code tooling
.claude/

# NOTE: media-source/ (original client photography, logo artwork and generated
# images) IS committed on purpose — scripts/build-media.mjs needs it to rebuild
# the optimised WebP files in public/images.
